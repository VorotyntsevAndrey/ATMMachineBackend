spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/atmdatabase
    driverClassName: org.postgresql.Driver
    password: vorotincevandrey87654321111111
  liquibase:
    enabled: true
    change-log: classpath:db.changelog/db.changelog-master.xml
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true

